---
'openzeppelin-confidential-contracts': minor
---

`ERC7984ERC20Wrapper`: Allow developers to associate 12 bytes of metadata with an unwrap request. This gets packed into the storage slot that is already being used to store the unwrap request recipient.
